# G1 Scope 与事务基础（关卡记录）

> 依据 plan.md §20 关卡表。出口条件：归属/恢复/尾损坏/幂等/沙箱测试全通过。

## 改了什么

| 文件 | 变化 |
|---|---|
| `core/atomic.py` | ✚ `append_line_sync`（journal append 原语：完整行+flush+fsync+新建时父目录同步）、`fsync_dir` |
| `agents/knowledge/scope_primitives.py`（新增） | M5 纯函数：`chapter_closure`（自 API 路由提炼，行为一致）、`volume_scoped_subgraph`（卷级闭包，无归属旧节点不放行）、`graph_content_revision`/`concept_revision`/`scope_revision_for`（§5.2 内容指纹，非 mtime） |
| `api/v1/knowledge.py` | 本地 `_chapter_closure` 改为 import 共享原语（零行为变化，test_knowledge_graph_views 全绿） |
| `agents/student_model/evaluation/store.py`（新增） | 每学生 journal：`append`（锁内分配 seq+checksum+fsync）、尾损坏截断恢复、中部损坏 `JournalCorruptError` 阻断、`GenerationConflictError`、`rewrite`（永久删除的 generation 重写，物理去内容）、重放状态机（20 种 op）、进程缓存 `get_journal`/`reset_journal_cache`/`purge_journal_files` |
| `agents/student_model/evaluation/scope.py`（新增） | `ScopeResolver`：§5.1 八步流程；归属 404 先于检索；只认注册教材；卷级子图；仅 concept 可评价；跨教材同名不合并；stamp 缓存分区 |
| `agents/student_model/evaluation/ports.py`（新增） | WorkspaceReader/TextbookReader/GraphReader/ContextReader/EvaluationReader 协议（评价核心不 import API/M3/M9） |
| `agents/student_model/evaluation/jobs.py`（新增） | `JobScheduler`：enqueue/claim_next（优先级+lease）/fail（retryable→retry_wait 退避）/cancel/cancel_scope_jobs/lease_valid（stale lease 拒绝提交；过期 running 可重认领=崩溃恢复） |
| `agents/student_model/evaluation/projections.py`（新增） | `rebuild_index`（删缓存后重放相同）、`load_index`（水位校验）、`concept_views`（scope 左连接 not_observed）、`workspace_summary`（coverage 四态+reconciling） |
| `core/learner_runtime.py`（新增） | composition root：默认适配器注入 ScopeResolver、JobScheduler 单例、`evaluation_enabled`、`reset_learner_runtime` |
| `core/config.py` | ✚ §10.3 预算配置（mode=active、concurrency=2、wall 45s、transport≤4、budget 120s、lease 150s、merge 15s、CLT 20%） |
| `tests/storage_sandbox.py` | 登记评价 journal 存储根 patch；`reset_shared_caches` 清 journal/scope/scheduler 缓存 |

## 唯一来源是否闭合

- 学习评价写入唯一路径 = `evaluation/store.py::EvidenceJournal.append`（所有 op 经
  JournalTransaction envelope）。G1 阶段尚无旧链删除（G2-G4 收口），本关新增
  写入全部走 journal。
- `students/` 根已按 owner 前缀扫描（orphan_cleanup 现有逻辑覆盖
  `*.learning_evidence.jsonl`/`.index.json`），无需新类别。

## 身份/范围和引用如何验证

- 身份：scope resolve 在任何检索前 404 外部 workspace（test_foreign_workspace_404）。
- 范围：选 f1 不进 f2（test_volume_scoping_selecting_volume_1_only）；
  无卷归属节点不放行（TestVolumePrimitive）；普通文件排除（test_plain_file…）。
- revision：定义变化 → scope_revision 变（test_scope_revision_changes…）；
  显示字段变化 → concept_revision 不变（test_concept_revision_stable…）。
- 幂等：同 source_revision 重复 result_committed 只保留一个当前解释
  （test_duplicate_result_commit_replaces_current_interpretation）。

## 测试证据

- `tests.test_learner_scope`（9）+ `tests.test_evidence_journal`（12）+
  `tests.test_evaluation_jobs`（12）= 33 例 OK。
- 回归：`test_knowledge_graph_views/index` OK；全量 discover 1979 例 OK
  （4 skip 为既有部署手册类跳过）。

## 已知限制（后续关卡补齐）

- evaluator/validator/service（提交前校验、LLM 调用）在 G2-G3。
- §5.3 生命周期的会话移动/回收站联动在 G3 lifecycle；本关已有 cancel_scope_jobs
  与 rewrite/purge 原语。
- synthesis（C7）与消费投递（consumer_ack 已入 envelope，M9 消费在 G4）。
