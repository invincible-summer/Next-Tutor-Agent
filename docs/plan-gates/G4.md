# G4 教学消费与记忆（关卡记录）

> 依据 plan.md §20 关卡表。出口条件：M1/M2/M3/M5/M6/M7/M9/M10 全部改读
> 新评价；无能力旁路/全局水平注入；M9 task/SRS 闭环不退化。旧评价算法/
> API/派生数据本版全部退出（§15.2），不留"下一版再清"。

## 改了什么

### 旧链删除（13 文件）

`student_model/{mastery,capability_projection,events,skill_graph,skill_graph_seed,adaptation}.py`、
`evaluation/learning_gain.py`、`assessment/{evaluator,structured_evaluator,session_store,continuation_policy}.py`、
`skill_runtime/evidence.py`、`core/{bloom_profile,learning_records,quiz_recent,error_notebook}.py`。

### 消费方改读统一评价

| 消费方 | 变化 |
|---|---|
| `student_model` | 重写为 profile facade（身份/学段/偏好；DEFAULT_STUDENT_ID 留在 store；blob 兼容读旧字段、写出 v2 profile-only） |
| `teaching_engine` | TeachingContext 删 mastery 字段、新增 `band_hint`（not_observed→novice / emerging·fragile·conflicting→progressing / supported_in_scope→solid(+strong)）；strategy/policy 按 band 四路选择；`_quiz_level_for_band`；难度中性起点 `neutral_seed()`=2 + CHALLENGE hard 兜底；curriculum 复习门改语义（调用方按 state 预筛，staleness 只排序） |
| supervisor | 快照融合行删除；`_adapt_via_engine` 中性 TeachingContext（评价经 understanding.evaluation_context）；curriculum 候选清空；数值读点（`_read_mastery_for` 等）全删；M7 evaluate_turn 不再传增益；[知识智能] 前置补缺读 journal 投影（`_journal_evaluation_view`） |
| M5 knowledge | graph/concepts 节点 `evaluation={state,statement,judgment_id,...}` 替换 mastery 块；`_evaluation_overlay(student_id, workspace_id)`（无 workspace 不着色，§11.6）；context_builder `mastery_view` → `evaluation_view`（前置"待补"= fragile/conflicting/emerging；未观察 ≠ 未掌握） |
| M7 evaluation | manager/schema/trace_analyzer 去 learning_gain/数值字段与分支；eval_traces 写回链修复 |
| M9 orchestration | 全面语义化：`_evaluation_view_safe`（journal 当前判断，裸 concept_id 键、多区取最不利）；GapItem/GoalState/Milestone/PlanConcept 数值字段删除（supported_ratio/支持·待解决·未观察分组）；LearningGoal/WeekTask/DailyTask/ReviewItem ✚ workspace_id（卡片键 (workspace_id, concept_key)）；`_graph_for_safe` 系（names/prereq/subject skills/概念链/计划输入）全部改读 M5 合并图谱；估期/进度按 supported 占比 |
| M9 闭环（§6.6/§13.8） | `consume_evaluation_outbox`：consumer="m9" 幂等领取 task_result（→SRS quality+绑定任务完成）/ review_resolved·interpretation_revoked（→受影响复习卡日期重放）→ `OpConsumerAck` 落盘；M9 侧 attempt 去重与 SRS/task 状态同一次原子写；evaluate_submission 只在 verdict 非 null 时投递；复核 INVALIDATE 与 lifecycle 撤销均投递（不只正向新增） |
| 生命周期/清理 | trash `_detach_learning_source_ids`、account_data `_forget_chat_memory` → `lifecycle.delete_session_sources`（§5.3 永久删除语义）；`purge_account` 接 `purge_journal_files`；quiz_attempts 删 learning_records/M3 复制/M9 推送（M3 直读新投影 §6.6；M9 走 outbox） |
| 读侧 API | /student：mastery/evidence-profile/bloom-profile/learning-records 路由删除（404，§19.1）；learning-path 重写（M5 frontier × 评价投影；review=已观察待解决；难度仅受近期作答影响）；error-notebook → journal 投影 `wrong_answer_items`；/quiz/recent journal 投影 |
| 其余 | StudentSnapshot 删 weak/strong/数值映射；AssessmentContext 删 current/target 数值与 STATUS_* 概念状态；generator 删 bloom_profile 上下文；activity_aggregator 学习活跃日读 journal；notes_agent 错题素材读 journal 投影；`ConceptState` → `ConceptEvalState`（§19.1 零 token） |

### goal API

GoalBody/GoalPatchBody ✚ workspace_id（§13.8 目标工作区归属；空 = unassigned，不自动归属）；task_binding ✚ workspace_id/goal_id/evaluation_scope_revision。

## 唯一来源是否闭合

- learner 写路径唯一：所有学生评价写入只经 evaluation service/store → journal；M1/M3/M5/M6/M7/M9/API/工具零直接 append。
- M9 不再是"第三个学习评价写入口"：判分端点只投递事件，M9 以 outbox 消费者身份幂等领取（consumer_ack 记录状态）。
- M3/M5/M6/M9/memory 页面同一 `EvaluationReader`/journal 投影；无人从 quiz_history、teaching_log 或正误次数重造学生水平。

## §19.1 静态扫描（backend/app）

```
p_known|MasteryTracker|ConceptState|record_quiz_result|rebuild_mastery|
derive_concept_status|mastery_map|current_mastery|target_mastery|
planned_mastery|mastered_ratio|before_mastery|after_mastery|
avg_learning_gain|seed_from_mastery|KnowledgeNodeMastery|MasteryResp|masteryColor
→ 0 命中
/student/(mastery|evidence-profile|bloom-profile|learning-records) → 0 命中（路由未注册）
```

## 测试证据

新增：test_learning_consumers(5)（消费 ack 幂等、verdict=null 不可观察、
只完成绑定任务、撤销重放复习卡、未知 kind 直接 ack）。
重写/更新：test_teaching_engine（band/难度/课程复习门语义化）、
test_orchestration（评价视图/无 fake mastery 键/needs_replan journal 种子）、
test_knowledge（evaluation_view 语义）、test_projection_api（evaluation 字段、
learning-path frontier、/mastery 404 合同）、test_quiz_grounding_provenance
（journal TaskSnapshot.grounding_refs）、test_admin_public（evaluation 隔离）、
test_knowledge_graph_index（journal/graph_for 命名空间）。
删除：测已删算法的旧测试（skill_graph 缓存类、ledger 最近题等，随 G4 各步）。
全量 discover：**1676 例 OK（skip=4）**；`create_app()` 可导入。

## 已知限制（后续关卡）

- G5 前端仍引用旧 mastery 字段/RadarCard 等——前端换装与 evaluation-labels 在 G5。
- demo 账号旧文件迁移、repository invariant 扫描扩展（含前端 token）在 G6。
- live provider 实测与 E2E 矩阵在 G7；本关卡全部 fake runner（零网络）。
- M9 消费目前由判分写回路径触发；独立 outbox 重投 sweep 循环随 G7 验收补（幂等语义已具备）。
