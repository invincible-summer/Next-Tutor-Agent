# G2 M4 统一链（关卡记录）

> 依据 plan.md §20 关卡表。出口条件：联合阅卷实际调用、旧 raw_grade 不可达、
> indeterminate 全链正确。

## 改了什么

| 文件 | 变化 |
|---|---|
| `prompts/learner_evaluation.py`（新增） | P0–P10 注册文本（plan §9 原文）；`quiz_blueprint@2.0.0`、`teaching_decision@2.0.0` 升版；P3/P4 情景文本表 |
| `prompts/registry.py` | 尾部 import 新模块（单一注册点保持） |
| `core/quiz_design.py` | P1 v2：system=P0+P1、业务信息（topic/学段锚点/focus/避让题/教材证据）全进 user JSON；`parse_blueprint`/`_render` 按 TaskBlueprint |
| `core/quiz_verify.py` | P2 逐题审核（A06）：`verify_questions` 输出 per-question `passed/rejected/revision_required/unreviewed`；缺项=unreviewed 仍交付但明确标记；set 级 `answer_verified` 要求全部 passed；`unreviewed_count` 计数 |
| `core/llm_async.py` | `AsyncLLMClient` 可选 `sdk_max_retries/retry_max/retry_base_delay`（评价 runner 专用，默认行为不变）；`complete(..., return_finish_reason=True)` |
| `evaluation/llm.py`（新增） | `EvaluationLLMRunner`：进程级并发闸、transport 重试统一（≤4 次）、错误分类（empty/truncated/timeout/rate/connection）、至多一次 P10 repair、`build_system_message`（P0+角色+情景+schema） |
| `evaluation/context.py`（新增） | ContextPack 组装（§8.1 分层）：allowlist c1..c12、prior_same_concept（保留反证）、帮助事件、manifest+input_hash |
| `evaluation/validator.py`（新增） | 唯一资格门（§17.2）：硬错误（伪引文/错 offset/白名单外概念/过期 source/claim 双处置/active claims 遗漏）+ 机会门（MC 无解释不评推理/无延迟不评保持/无基线不评迁移）+ 发布门（§4.4 五类状态资格） |
| `evaluation/service.py`（新增） | 提交 facade：lease/generation/scope 再检查、validator 门、ConceptUpdate→ConceptJudgment 物化（obs/clm/jdg 服务端 ID 分配、retain/revise/close）、单 `result_committed` 事务 + synthesis dirty outbox |
| `evaluation/grading.py`（新增） | `grade_mc_task`（确定性 MC）/`compute_task_result`（冻结量规加权；关键 criterion 未观察→indeterminate，score=null；N/A 分母规则） |
| `agents/assessment/manager.py`（重写） | `evaluate_submission` 唯一入口（§11.4 契约签名）：完整答案指纹判重、同题异答 409、MC 判分随受理事务先行落盘、帮助事件从 journal 读取、`run_assessment_job`（pack→P3→validator→commit，CommitRejected→job failed）；`task_snapshot_from_quiz_dict/legacy` 转换器 |
| `agents/assessment/adaptive_test.py`（重写） | CAT journal 投影版（A10）：CatInstance 经 `assessment_session_changed` 持久化；难度步进排除 null verdict；停止只做硬上限；P3 continuation continue/probe/finish；report 分清 pending |
| `agents/assessment/__init__.py` | 新出口（evaluate_submission 等）；删除 evaluate_and_record/parse_grade/derive_concept_status/evaluate_mc/raw_grade 旧出口 |
| `api/v1/assessment.py`（重写） | §11.4：POST/GET submissions、hint/reveal/questions/practice/records；§11.5：start（workspace 归属 404 先行 + 教材 grounding 经已选教材）/answer/next（不叠题、evaluation_pending 409）/active/report/abandon（幂等）；§11.1 错误 envelope |
| `api/v1/quiz.py`（重写） | 薄适配层：question_id+revision 身份、非流式正式反馈（§10.4）、写回按 question_id、hint/dispute（review_requested 登记 + review job）、recent 从 journal 投影；旧 stem/correct_answer/raw_grade/record 契约删除（422） |
| `agents/executor.py` | 出题落 quiz_history 时注册 TaskSnapshot 到 journal（概念严格匹配工作区 scope） |
| `core/learner_runtime.py` | ✚ runner 单例（get/set/reset evaluation runner） |

## 旧链退出（本关）

- 删除文件：`agents/assessment/evaluator.py` 引用清零（parse_grade/grade_open_prompt 不可达）、`structured_evaluator.py`（assessment_analyze 评价用途终止）、`session_store.py`（单槽 .assessment.json 终止）、`continuation_policy.py`（并入 P3 continuation + adaptive_test）。**物理删除在 G6 与全库扫描一起收口**；本关先断全部调用方。
- 删除测试：test_structured_assessment / test_structured_v2_2 / test_grade_auto / test_m58_bridge / test_quiz_evidence_feed / test_quiz_writeback / test_continuation_policy（旧算法专属）。

## 唯一来源是否闭合

所有作答受理（chat/center/CAT）→ `evaluate_submission` → journal（source_registered+job_requested 同事务 fsync）。旧写入链（record_quiz_attempt M6/M3/M9 扇出、record_quiz_result BKT、learning_records 账本）不再被新受理路径触达；其调用点仅剩 executor 出题快照（quiz_recent/learning_records 只录题目不录判分）——完整删除在 G4/G6。

## 身份/范围和引用如何验证

- 身份：`test_assessment_identity`（JWT 键定 journal；body student_id/raw_grade 422）。
- 范围：start 的 workspace 归属 404 先于检索（`test_workspace_cat_grounding::test_foreign_workspace_404`）。
- 引用：validator 硬门（`test_unified_submission::TestValidatorGate`：伪引文 job failed 无判断；白名单外概念拒收）。
- 题目身份：`test_submission_identity`（同前缀不同 id 独立、答案 200 字前缀不合并、revision 冲突、跨区 CAT 归档正确）。

## 测试证据

- 新增/重写：test_unified_submission(13)、test_submission_identity(4)、test_question_audit(6)、test_assessment(10)、test_assessment_identity(4)、test_assessment_lifecycle(6)、test_quiz_ownership(8)、test_workspace_cat_grounding(3)、test_quiz_quality(47)/test_quiz_design(13)/test_quiz_grounding×2(20) 全量按 P1/P2 契约更新。
- 全量 discover：**OK（skip=4）** —— 修复一处测试污染（lifecycle 覆盖沙箱 _env_old 致 AUTH_MODE 泄漏）。

## 已知限制（后续关卡）

- P3/P10 未接 live provider（离线 fake 验证；live harness 是 G7）。
- 对话来源（C5/P4）、综合（C7/P9）、复核 job 执行（C9/P5）在 G3。
- executor 的 record_recent_quiz/record_generated_quiz 旧快照链在 G4/G6 改 journal 投影。
- M9/M6 消费（outbox consumer_ack）在 G4。
